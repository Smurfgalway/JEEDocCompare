package ie.gmit.sw;

public class DocumentDatabase {
/*
 * this should contain the db40 database set up and call the document contents
 * it is called by the compareDoc when looking for files to compare against the users selected document
 * on the jsp interface. 
 * 
 * 
 */
}
